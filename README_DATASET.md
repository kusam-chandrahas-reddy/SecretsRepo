# Enterprise Secret-Scanner Benchmark Dataset (Synthetic)

This dataset contains multiple file types with embedded *synthetic* secret patterns.
It includes Salesforce-specific secret shapes (Client ID/Secret, Refresh Token, Session ID) and secrets placed in both comments and active code.
Use for accuracy benchmarking only; values are not valid credentials.
